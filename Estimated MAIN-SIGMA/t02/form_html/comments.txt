* рядок №9 потрібно додати атрибут lang до початкового тегу html, щоб оголосити мову цього документа.

* рядок №36 атрибут size не дозволений в елементі input type="number".

* рядок №68 для елементу input type="image" відсутній необхідний атрибут alt для картинки.

* в input має бути доданий атрибут "id", так як атрибут "for" тегу label відповідає ідентифікатору елемента, а не його імені.

* тегом <b> було б доцільніше огортати лише текст, а не весь label (це покращує візуальне сприйняття коду)

* починаючи з HTML4, значення тегу <b> було змінено і для стилізації тексту доцільно використовувати властивості CSS, а тег <b> - для виділення ключових слів
 