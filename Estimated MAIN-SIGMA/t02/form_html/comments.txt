* потрібно додати атрибут lang до початкового тегу html, щоб оголосити мову цього документа.

* має бути доданий id в елемент input, так як атрибут for відповідає ідентифікатору елемента, а не його імені.

* атрибут size не дозволений в елементі input.

* для елементу input  відсутній необхідний атрибут alt для картинки.
